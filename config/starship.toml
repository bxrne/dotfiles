format = """
$username$hostname$directory$git_branch$git_status$git_state$python$cmd_duration$character
"""
add_newline = false

[directory]
 style = "bold #abb2bf"
   truncation_length = 3
  truncate_to_repo = true
  format = "[$path]($style) "

[character]
  success_symbol = "[∷](bold #d19a66)"
  error_symbol = "[∷](bold #e06c75)"
  vimcmd_symbol = "[∷](bold #d19a66)"

[git_branch]
  symbol = " "                        # branch symbol
  format = "[$symbol$branch]($style) "
  style = "bold #d19a66"

[git_status]
  format = "([$all_status$ahead_behind]($style)) "
  style = "#d19a66"
  conflicted = "="
  untracked = "?"
  modified = "!"
  staged = "+"
  renamed = "»"
  deleted = "✘"
  stashed = "≡"

[git_state]
  format = "[$state( $progress_current/$progress_total)]($style)"
  style = "bold #d19a66"

[cmd_duration]
  min_time = 500
  format = " [$duration]($style) "
  style = "#5c6370"

[python]
  symbol = "  "                          # Python logo glyph
  format = "[$symbol$virtualenv]($style)"
  style = "#d19a66"

[username]
  style_user = "bold #5c6370"
  style_root = "bold #e06c75"
  format = "[$user]($style)@"
  disabled = false
  show_always = true

[hostname]
  ssh_only = true
  format = "📡 [$hostname]($style) "
  style = "bold #5c6370"
  disabled = false
