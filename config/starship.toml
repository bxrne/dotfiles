format = """
$username$hostname$directory$git_branch$git_status$git_state$python$cmd_duration$character
"""
add_newline = false

[directory]
style = "bold #a7c080"
truncation_length = 3
truncate_to_repo = true
format = "[$path]($style) "

[character]
success_symbol = "[∷](bold #a7c080)"
error_symbol = "[∷](bold #e67e80)"
vimcmd_symbol = "[∷](bold #87c095)"

[git_branch]
symbol = " "                        # branch symbol
format = "[$symbol$branch]($style) "
style = "bold #a7c080"

[git_status]
format = "([$all_status$ahead_behind]($style)) "
style = "#a7c080"
conflicted = "="
untracked = "?"
modified = "!"
staged = "+"
renamed = "»"
deleted = "✘"
stashed = "≡"

[git_state]
format = "[$state( $progress_current/$progress_total)]($style)"
style = "bold #e67e80"

[cmd_duration]
min_time = 500
format = " [$duration]($style) "
style = "#859289"

[python]
symbol = "  "                          # Python logo glyph
format = "[$symbol$virtualenv]($style)"
style = "#87c095"

[username]
style_user = "bold #a7c080"
style_root = "bold #e67e80"
format = "[$user]($style)@"
disabled = false
show_always = true

[hostname]
ssh_only = true
format = "📡 [$hostname]($style) "
style = "bold #87c095"
disabled = false
